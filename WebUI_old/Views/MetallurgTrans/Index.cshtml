@model IEnumerable<EFRailWay.Entities.MTSostav>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@foreach (var p in Model)
{
    <div class="item">
        <h3>@p.CompositionIndex</h3>
        @p.FileName
        <h4>@p.DateTime</h4>
    </div>
}
